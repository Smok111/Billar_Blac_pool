@model BillarBlackPool.Models.Rol

@{
    ViewData["Title"] = "Nuevo Rol";
}

<link href="https://fonts.googleapis.com/css2?family=Fredoka+One&family=Nunito:wght@400;700;900&display=swap" rel="stylesheet" />
<script src="https://cdn.lordicon.com/lordicon.js"></script>
<link href="~/css/Cliente.css" rel="stylesheet" />

<div class="container mt-5 mb-5">
    <div class="row justify-content-center">
        <div class="col-lg-8 col-md-10">

            <!-- Card del formulario -->
            <div class="cliente-form-card">

                <!-- Header -->
                <div class="cliente-form-header">
                    <div class="cliente-form-icon-wrapper">
                        <lord-icon src="https://cdn.lordicon.com/mecwbjnp.json"
                                   trigger="loop"
                                   colors="primary:#6CA8FF,secondary:#76E6A3"
                                   style="width:70px;height:70px">
                        </lord-icon>
                    </div>
                    <h2 class="cliente-form-title mb-2">Registrar Nuevo Rol</h2>
                    <p class="cliente-form-subtitle">Complete los datos del rol</p>
                </div>

                <!-- Formulario -->
                <form asp-action="Create" method="post" class="cliente-form" id="formRol">
                    @Html.AntiForgeryToken()

                    <div class="row g-4">

                        <!-- Nombre del Rol -->
                        <div class="col-12">
                            <div class="cliente-field">
                                <label asp-for="NomRol" class="cliente-label">
                                    <lord-icon src="https://cdn.lordicon.com/kthelypq.json"
                                               trigger="hover"
                                               colors="primary:#6CA8FF"
                                               style="width:24px;height:24px">
                                    </lord-icon>
                                    <span>Nombre del Rol</span>
                                    <span class="required-asterisk">*</span>
                                </label>
                                <div class="input-wrapper">
                                    <input asp-for="NomRol"
                                           class="cartoon-input"
                                           placeholder="Ej: Administrador, Usuario, Moderador"
                                           required
                                           autocomplete="off" />
                                    <div class="input-icon">
                                        <lord-icon src="https://cdn.lordicon.com/jxzkkoed.json"
                                                   trigger="hover"
                                                   colors="primary:#6CA8FF"
                                                   style="width:20px;height:20px">
                                        </lord-icon>
                                    </div>
                                </div>
                                <span asp-validation-for="NomRol" class="validation-message"></span>
                            </div>
                        </div>

                        <!-- Descripción -->
                        <div class="col-12">
                            <div class="cliente-field">
                                <label asp-for="DesRol" class="cliente-label">
                                    <lord-icon src="https://cdn.lordicon.com/abvsilkn.json"
                                               trigger="hover"
                                               colors="primary:#6CA8FF"
                                               style="width:24px;height:24px">
                                    </lord-icon>
                                    <span>Descripción</span>
                                    <span class="required-asterisk">*</span>
                                </label>
                                <div class="input-wrapper">
                                    <textarea asp-for="DesRol"
                                              class="cartoon-input"
                                              placeholder="Descripción de las funciones y permisos del rol..."
                                              rows="4"
                                              required
                                              autocomplete="off"></textarea>
                                    <div class="input-icon">
                                        <lord-icon src="https://cdn.lordicon.com/abvsilkn.json"
                                                   trigger="hover"
                                                   colors="primary:#6CA8FF"
                                                   style="width:20px;height:20px">
                                        </lord-icon>
                                    </div>
                                </div>
                                <span asp-validation-for="DesRol" class="validation-message"></span>
                            </div>
                        </div>

                    </div>

                    <!-- Información adicional -->
                    <div class="cliente-info-box mt-4">
                        <lord-icon src="https://cdn.lordicon.com/abfverha.json"
                                   trigger="loop"
                                   colors="primary:#6CA8FF"
                                   style="width:28px;height:28px">
                        </lord-icon>
                        <div>
                            <strong>Información:</strong> Los roles definen los permisos y acceso a funciones del sistema para los usuarios.
                        </div>
                    </div>

                    <!-- Botones de acción -->
                    <div class="cliente-form-actions mt-5">
                        <a asp-action="Index" class="cliente-btn-cancel cartoon-btn-secondary">
                            <lord-icon src="https://cdn.lordicon.com/nqtddedc.json"
                                       trigger="hover"
                                       colors="primary:#ffffff"
                                       style="width:22px;height:22px">
                            </lord-icon>
                            <span>Cancelar</span>
                        </a>

                        <button type="submit" class="cliente-btn-submit cartoon-btn-primary">
                            <lord-icon src="https://cdn.lordicon.com/lomfljuq.json"
                                       trigger="hover"
                                       colors="primary:#ffffff"
                                       style="width:22px;height:22px">
                            </lord-icon>
                            <span>Registrar Rol</span>
                        </button>
                    </div>

                </form>

            </div>

            <!-- Enlace de retorno adicional -->
            <div class="text-center mt-4">
                <a asp-action="Index" class="cliente-back-link">
                    ← Volver a la lista de roles
                </a>
            </div>

        </div>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }

    <script>
        // Validación mejorada en tiempo real
        document.getElementById('formRol').addEventListener('submit', function(e) {
            const inputs = this.querySelectorAll('input[required], textarea[required]');
            let isValid = true;

            inputs.forEach(input => {
                if (!input.value.trim()) {
                    isValid = false;
                    input.classList.add('input-error');
                } else {
                    input.classList.remove('input-error');
                }
            });

            if (!isValid) {
                e.preventDefault();
            }
        });

        // Efecto de focus en inputs
        document.querySelectorAll('.cartoon-input').forEach(input => {
            input.addEventListener('focus', function() {
                this.parentElement.classList.add('input-focused');
            });

            input.addEventListener('blur', function() {
                this.parentElement.classList.remove('input-focused');
            });
        });
    </script>
}